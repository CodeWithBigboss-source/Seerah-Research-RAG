# requirements-colab.txt
# Only packages Colab does NOT pre-install
# NO version pins — let Colab's resolver handle compatibility
# NO torch, transformers, langchain, sentence-transformers — already installed
# NO fastapi, uvicorn, pydantic, requests, starlette — Google owns these on Colab

chromadb
rank-bm25
pymupdf
pytesseract
pillow
langchain-text-splitters
langchain-huggingface
langchain-community